#include<iostream> 
using namespace std; 
int fun(int num1,int num2) 
{  
   int count=0; 
   for(int i=num1;i<=num2;i++) 
   {  
      if(i%num1==0) 
         count++;
   } 
   return count;
} 
int main() 
{  
   int num1,num2; 
   cin>>num1>>num2; 
   if(num1>num2) 
      cout<<"-1"; 
   else 
     cout<<fun(num1,num2);
}
